import React from 'react';
import styled from '@emotion/styled';

const Root = styled.div`
  height: 100%;
`;

const PipelinePage = (): JSX.Element => {
  return (
    <Root />
  );
};

export default PipelinePage;
